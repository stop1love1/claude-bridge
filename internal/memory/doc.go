// Package memory persists task/session memory, distills long
// transcripts into compact context, and tracks pinned files plus the
// "recent direction" the operator last steered the session toward.
package memory
