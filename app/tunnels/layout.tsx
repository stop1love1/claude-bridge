import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tunnels",
};

export default function TunnelsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
