# Tasks (legacy notebook — not runtime data)

> Runtime task state lives in `sessions/<task-id>/meta.json`, written by the bridge UI / API.
> This file is no longer read or updated by the bridge — keep it only as a free-form notebook
> for ideas you want out of the UI's task list. The coordinator does NOT consume it.

_(empty)_
